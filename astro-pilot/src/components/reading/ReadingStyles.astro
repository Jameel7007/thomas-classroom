<style is:inline>
  .reading-lab-page .mast-inner,.reading-page .mast-inner{padding-top:24px;padding-bottom:22px}
  .reading-lab-page .title,.reading-page .title{font-size:clamp(35px,5vw,56px)}
  .reading-lab-page .mast-stats,.reading-page .mast-stats{display:none}
  .reading-library,.reading-shell{max-width:1160px;margin:0 auto;padding:42px var(--pad) 80px}
  .reading-library-intro{display:grid;grid-template-columns:minmax(0,1.05fr) minmax(310px,.65fr);gap:36px;align-items:end;margin-bottom:38px}
  .reading-library-intro h2,.reading-section-head h2{margin:0;color:var(--ink);font-family:var(--display);font-size:clamp(34px,5vw,52px);font-weight:500;letter-spacing:-.025em;line-height:1}
  .reading-library-intro p,.reading-section-head>p:last-child{margin:12px 0 0;color:var(--ink-2);font-size:17px;line-height:1.55}
  .reading-overline{margin:0 0 9px;color:var(--accent-deep);font:600 11px/1.4 var(--mono);letter-spacing:.11em;text-transform:uppercase}
  .reading-principles{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-bottom:34px}
  .reading-principles div{padding:18px;border:1px solid var(--line);border-radius:13px;background:var(--surface)}
  .reading-principles b{display:block;margin-bottom:5px;font-family:var(--display);font-size:20px;font-weight:550}.reading-principles span{display:block;color:var(--ink-2);font-size:13px;line-height:1.5}
  .reading-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:15px}
  .reading-card{display:flex;min-width:0;min-height:300px;flex-direction:column;padding:26px;border:1px solid var(--line);border-radius:17px;background:var(--surface);color:var(--ink);text-decoration:none;box-shadow:0 18px 40px -36px var(--cobalt-deep);transition:transform .16s ease,border-color .16s ease,box-shadow .16s ease}
  .reading-card:hover{transform:translateY(-3px);border-color:var(--accent);box-shadow:0 22px 40px -30px var(--accent-deep)}.reading-card:focus-visible{outline:3px solid var(--focus-ring);outline-offset:3px}
  .reading-card[data-level="A0"],.reading-card[data-level="A1"]{background:linear-gradient(145deg,var(--surface),var(--accent-tint))}.reading-card[data-level="B2"],.reading-card[data-level="C1"]{background:linear-gradient(145deg,var(--surface),var(--cobalt-tint))}
  .reading-card-meta{display:flex;align-items:center;justify-content:space-between;gap:12px;color:var(--muted);font:600 10.5px/1.4 var(--mono);letter-spacing:.07em;text-transform:uppercase}.reading-card-level{color:var(--accent-deep);font-size:14px}
  .reading-card h2{margin:30px 0 10px;font-family:var(--display);font-size:clamp(28px,3vw,36px);font-weight:520;line-height:1.04}.reading-card p{margin:0;color:var(--ink-2);font-size:15px;line-height:1.58}
  .reading-card-tags{display:flex;flex-wrap:wrap;gap:7px;margin-top:auto;padding-top:24px}.reading-card-tags span{padding:6px 9px;border:1px solid var(--line);border-radius:999px;background:var(--paper);color:var(--ink-2);font:500 10px/1.3 var(--mono)}
  .reading-card-action{display:block;margin-top:16px;color:var(--accent-deep);font:650 11px/1.4 var(--mono);letter-spacing:.06em;text-transform:uppercase}
  .reading-breadcrumb{display:flex;align-items:center;gap:8px;margin-bottom:22px;color:var(--muted);font:600 10.5px/1.4 var(--mono);letter-spacing:.07em;text-transform:uppercase}.reading-breadcrumb a{color:var(--ink);text-underline-offset:3px}.reading-breadcrumb a:focus-visible{outline:2px solid var(--focus-ring);outline-offset:3px}
  .reading-intro-card{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:22px;align-items:end;margin-bottom:28px;padding:24px 26px;border:1px solid var(--line);border-radius:16px;background:var(--surface)}
  .reading-intro-card h2{margin:0;font-family:var(--display);font-size:28px;font-weight:520}.reading-intro-card p{max-width:68ch;margin:8px 0 0;color:var(--ink-2);line-height:1.55}.reading-meta{display:flex;flex-wrap:wrap;gap:7px;margin-top:15px}.reading-meta span{padding:6px 9px;border:1px solid var(--line);border-radius:999px;background:var(--paper);font:500 10.5px/1.3 var(--mono)}
  .reading-tutor-link{display:inline-flex;min-height:44px;align-items:center;justify-content:center;padding:9px 13px;border:1px solid var(--ink);border-radius:9px;background:var(--ink);color:var(--paper);font:600 10.5px/1.3 var(--mono);letter-spacing:.05em;text-decoration:none;text-transform:uppercase}.reading-tutor-link:focus-visible{outline:3px solid var(--focus-ring);outline-offset:3px}
  .reading-before{margin:0 0 34px;padding:26px 28px;border:1px solid var(--accent-soft);border-radius:16px;background:var(--accent-tint)}.reading-before h2{margin:0 0 13px;font-family:var(--display);font-size:29px;font-weight:520}.reading-before ol{margin:0;padding-left:23px}.reading-before li{margin:8px 0;padding-left:5px;color:var(--ink);font-size:17px;line-height:1.55}
  .reading-text-shell{max-width:880px;margin:0 auto 46px}.reading-text-guide{margin:0 0 18px;padding:13px 16px;border-left:3px solid var(--cobalt);background:var(--cobalt-tint);color:var(--ink-2);font-size:14px;line-height:1.5}
  .reading-copy{padding:clamp(28px,5vw,52px);border:1px solid var(--line);border-radius:18px;background:var(--surface);box-shadow:0 22px 50px -43px var(--cobalt-deep);color:var(--ink);font-family:var(--serif);font-size:clamp(19px,2.2vw,22px);line-height:1.78}
  .reading-copy>p:first-child{font-size:1.12em;line-height:1.68}.reading-copy p{margin:0 0 1.25em}.reading-copy p:last-child{margin-bottom:0}.reading-copy h2{margin:2em 0 .65em;font-family:var(--display);font-size:1.65em;font-weight:520;line-height:1.05}.reading-copy blockquote{margin:1.5em 0;padding:16px 20px;border-left:4px solid var(--accent);background:var(--accent-tint);font-style:normal}.reading-copy hr{height:1px;margin:2em 0;border:0;background:var(--line)}
  .reading-copy .reading-document{margin:1.35em 0;padding:20px;border:1px solid var(--line);border-radius:12px;background:var(--paper);font-family:var(--sans);font-size:.82em;line-height:1.6}.reading-copy .reading-document p{margin:.55em 0}.reading-copy .reading-message-head{display:grid;gap:3px;margin-bottom:16px;padding-bottom:12px;border-bottom:1px solid var(--line);font:500 .72em/1.5 var(--mono)}
  .reading-copy .reading-pullquote{display:block;margin:1.5em 0;color:var(--accent-deep);font-family:var(--display);font-size:1.35em;line-height:1.15;text-align:center}
  .reading-rights{margin:13px 0 0;color:var(--muted);font:500 10.5px/1.5 var(--mono)}.reading-rights p{margin:3px 0}.reading-rights a{color:var(--accent-deep);text-underline-offset:3px}.reading-rights a:focus-visible{outline:2px solid var(--focus-ring);outline-offset:3px}
  .reading-word-wrap{display:inline}.reading-word{display:inline;margin:0;padding:0 .08em;border:0;border-bottom:2px dotted var(--accent);background:transparent;color:var(--accent-deep);font:inherit;font-style:normal;cursor:pointer}.reading-word:hover{background:var(--accent-tint)}.reading-word:focus-visible{outline:3px solid var(--focus-ring);outline-offset:3px}
  .reading-word-help{inset:auto;max-width:min(360px,calc(100vw - 30px));padding:0;border:1px solid var(--ink);border-radius:12px;background:var(--surface);color:var(--ink);box-shadow:6px 6px 0 var(--ink);font-family:var(--sans);font-size:15px;line-height:1.5}.reading-word-help::backdrop{background:rgba(20,23,28,.18)}.reading-word-help>span{display:block;padding:0 17px 15px}.reading-word-help small{display:block;margin:0 17px 17px;padding-top:11px;border-top:1px solid var(--line);color:var(--ink-2);font-size:12px}.reading-word-help-head{display:flex!important;align-items:center;justify-content:space-between;padding:13px 13px 10px 17px!important}.reading-word-help-head strong{font-family:var(--display);font-size:24px;font-weight:550}.reading-word-help-head button{width:34px;height:34px;border:1px solid var(--line);border-radius:8px;background:var(--paper);color:var(--ink);font-size:20px;cursor:pointer}
  .reading-section{margin:64px 0}.reading-section-head{max-width:760px;margin-bottom:24px}.reading-section-head h2{font-size:clamp(32px,4vw,45px)}
  .reading-vocabulary-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.reading-vocabulary-card{padding:17px 18px;border:1px solid var(--line);border-radius:12px;background:var(--surface)}.reading-vocabulary-card h3{margin:0 0 5px;color:var(--accent-deep);font-family:var(--display);font-size:24px;font-weight:550}.reading-vocabulary-card p{margin:0;color:var(--ink-2);font-size:14px;line-height:1.5}.reading-vocabulary-card small{display:block;margin-top:10px;color:var(--muted);font:500 10.5px/1.45 var(--mono)}
  .reading-quiz{overflow:visible;margin:64px 0;padding:30px;border:1px solid var(--line);border-radius:18px;background:var(--surface);box-shadow:0 18px 42px -38px var(--cobalt-deep)}.reading-quiz .reading-section-head{margin-bottom:28px}.quiz-list{display:grid;gap:14px}.quiz-item{padding:18px;border:1px solid var(--line-2);border-radius:12px;background:var(--paper)}.quiz-question{display:flex;gap:12px;margin:0 0 13px;color:var(--ink);font-size:17px;line-height:1.5}.quiz-question>span{flex:none;color:var(--accent-deep);font:600 11px/1.7 var(--mono)}.quiz-options{display:flex;flex-wrap:wrap;gap:8px}.quiz-option{min-height:47px;padding:10px 14px;border:2px solid var(--ink);background:var(--surface);box-shadow:3px 3px 0 var(--ink);color:var(--ink);font:650 14px/1.35 var(--sans);cursor:pointer}.quiz-option:hover{background:var(--cobalt-tint)}.quiz-option:focus-visible,.lesson-btn:focus-visible{outline:3px solid var(--focus-ring);outline-offset:3px}.quiz-option.is-selected{border-color:var(--cobalt-deep);background:var(--cobalt);color:white}.quiz-option.is-correct{border-color:#26713a;background:#dff5e3;color:#144f24;box-shadow:3px 3px 0 #26713a}.quiz-option.is-wrong{border-color:#a8342b;background:#fde5e1;color:#7a241d;box-shadow:3px 3px 0 #a8342b}.drill-actions{display:flex;flex-wrap:wrap;gap:9px;margin-top:20px}.lesson-btn{min-height:48px;padding:10px 16px;border:2px solid var(--ink);background:var(--ink);color:var(--paper);font:650 11px/1.3 var(--mono);letter-spacing:.06em;text-transform:uppercase;cursor:pointer}.lesson-btn+ .lesson-btn{background:var(--surface);color:var(--ink)}.feedback{min-height:1.5em;margin:15px 0 0;color:var(--ink);font:600 13px/1.5 var(--mono)}.reading-quiz.is-success{border-color:#26713a}.reading-quiz.is-error{border-color:#a8342b}
  .reading-language-examples,.reading-craft-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.reading-language-example,.reading-craft-card{padding:19px;border:1px solid var(--line);border-radius:13px;background:var(--surface)}.reading-language-example strong,.reading-craft-card h3{display:block;margin:0 0 7px;color:var(--accent-deep);font-family:var(--display);font-size:22px;font-style:normal;font-weight:550}.reading-language-example p,.reading-craft-card p{margin:0;color:var(--ink-2);font-size:14px;line-height:1.55}.reading-craft-card blockquote{margin:12px 0 0;padding:11px 13px;border-left:3px solid var(--accent);background:var(--accent-tint);font-size:14px}
  .reading-response{margin:64px 0}.reading-response-list{display:grid;gap:9px}.reading-response-stage{border:1px solid var(--line);border-radius:13px;background:var(--surface)}.reading-response-stage>summary{display:grid;grid-template-columns:42px minmax(100px,.35fr) minmax(0,1fr);gap:10px 15px;align-items:center;padding:18px 20px;cursor:pointer;list-style:none}.reading-response-stage>summary::-webkit-details-marker{display:none}.reading-response-stage>summary>span{color:var(--accent-deep);font:650 12px/1.4 var(--mono)}.reading-response-stage>summary>strong{font-family:var(--display);font-size:22px;font-weight:550}.reading-response-stage>summary>small{color:var(--ink-2);font-size:14px}.reading-response-stage[open]>summary{border-bottom:1px solid var(--line);background:var(--accent-tint)}.reading-response-stage>summary:focus-visible,.reading-model>summary:focus-visible{outline:3px solid var(--focus-ring);outline-offset:3px}.reading-response-body{padding:20px}.reading-chunk-label{margin:0 0 9px;color:var(--muted);font:600 10px/1.4 var(--mono);letter-spacing:.07em;text-transform:uppercase}.reading-chunks{display:flex;flex-wrap:wrap;gap:7px}.reading-chunks span{padding:7px 10px;border:1px solid var(--line);border-radius:999px;background:var(--paper);font-size:13px}.reading-model{margin-top:16px;padding:13px 15px;border:1px dashed var(--line);border-radius:10px}.reading-model>summary{color:var(--accent-deep);font:600 11px/1.4 var(--mono);cursor:pointer;text-transform:uppercase}.reading-model p{margin:12px 0 0;color:var(--ink-2);font-size:15px;line-height:1.6}
  .reading-production{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin:64px 0}.reading-production article{padding:25px;border:1px solid var(--ink);border-radius:15px;background:var(--surface);box-shadow:4px 4px 0 var(--ink)}.reading-production article:last-child{border-color:var(--cobalt-deep);box-shadow:4px 4px 0 var(--cobalt-deep)}.reading-production h2{margin:0 0 9px;font-family:var(--display);font-size:30px;font-weight:550}.reading-production p{margin:0;color:var(--ink-2);line-height:1.6}.reading-production .reading-chunks{margin-top:16px}.reading-related{margin-top:60px;padding-top:28px;border-top:1px solid var(--line)}.reading-related h2{margin:0 0 15px;font-family:var(--display);font-size:31px;font-weight:550}.reading-related-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.reading-related a{display:grid;gap:4px;padding:16px;border:1px solid var(--line);border-radius:11px;background:var(--surface);text-decoration:none}.reading-related a:hover{border-color:var(--accent)}.reading-related span{color:var(--muted);font:600 10px/1.4 var(--mono);text-transform:uppercase}.reading-related strong{font-family:var(--display);font-size:19px;font-weight:550}
  .reading-tutor-page .mast-stats{display:none}.reading-tutor-plan{max-width:980px}.reading-tutor-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.reading-tutor-card{padding:21px;border:1px solid var(--line);border-radius:13px;background:var(--surface)}.reading-tutor-card h2{margin:0 0 9px;font-family:var(--display);font-size:25px;font-weight:550}.reading-tutor-card p,.reading-tutor-card li{color:var(--ink-2);font-size:14px;line-height:1.55}.reading-tutor-card ul,.reading-tutor-card ol{margin:0;padding-left:20px}.reading-answer-key{margin-top:34px}.reading-answer-key li{margin:10px 0}.reading-answer-key b{color:var(--accent-deep)}
  @media(max-width:800px){.reading-library-intro,.reading-intro-card{grid-template-columns:1fr}.reading-grid,.reading-principles,.reading-production{grid-template-columns:1fr}.reading-tutor-link{justify-self:start}.reading-language-examples,.reading-craft-grid,.reading-tutor-grid{grid-template-columns:1fr}}
  @media(max-width:600px){.reading-library,.reading-shell{padding-top:30px}.reading-vocabulary-grid,.reading-related-grid{grid-template-columns:1fr}.reading-copy{padding:25px 20px;font-size:19px}.reading-before,.reading-quiz{padding:21px 18px}.reading-response-stage>summary{grid-template-columns:34px minmax(0,1fr)}.reading-response-stage>summary>small{grid-column:2}.quiz-options{display:grid}.quiz-option{width:100%;text-align:left}.reading-card{min-height:0}.reading-principles{display:none}}
  @media(prefers-reduced-motion:reduce){.reading-card{transition:none}}
  @media print{.masthead,.topnav,.foot,.reading-tutor-link,.reading-quiz .drill-actions{display:none!important}.reading-shell,.reading-library{max-width:none;padding:0}.reading-copy,.reading-quiz,.reading-response-stage,.reading-production article{box-shadow:none;break-inside:avoid}.reading-word-help{display:none}.reading-tutor-grid{grid-template-columns:1fr 1fr}}
</style>
